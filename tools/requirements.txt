# Python dependencies for tools/knowledge_updater.py
#
# The updater runs with ZERO required dependencies via the stdlib urllib
# fallback (degraded mode). The packages below enable the higher-fidelity
# crawl4ai path for JavaScript-rendered pages. Pin for reproducibility.

crawl4ai>=0.3.7,<0.5.0   # optional: JS-rendered crawling for domain sources
